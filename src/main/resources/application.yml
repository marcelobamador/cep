server:
  port: 8080

spring:
  application:
    name: cep-lookup-api

via-cep:
  base-url: https://viacep.com.br

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always
